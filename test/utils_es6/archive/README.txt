usage:
$ node readfile [filename]   // no filename => reads itself
$ node readline [filename]   // no filename => reads itself

exp: $ node readfile ./06-15-53.log0 > e2e_spec.txt

NOTE: Each sends each line to stdout with a blank line between
